# Skills & Prompts 整理

> 分布式配置中心项目 — AI 协同研发的完整 Skill 使用和 Prompt 工程记录

---

## 一、使用的 AI 工具

| 工具 | 版本/环境 | 角色 |
|------|----------|------|
| Claude Code | VSCode Extension (Reasonix) | 全流程主引擎：设计、编码、测试、调试、文档 |
| Claude Agent SDK | Plan / Explore / general-purpose | 架构设计 Agent、代码探索 Agent |

---

## 二、Skill 使用记录

### 2.1 内置 Slash Command

| Skill | 调用方式 | 使用场景 | 效果 |
|-------|---------|---------|------|
| `EnterPlanMode` | 大规模改动前自动触发 | 多域仿真平台架构设计、前端左右分栏重设计 | 输出 Plan → 用户审批 → 顺序实施 |
| `Explore Agent` | 自动 spawn 子 Agent | 阅读 backend seed data、demo-service 结构 | 并行探索 2-3 个代码区域，返回结构化摘要 |
| `Plan Agent` | 显式调用 | 多域仿真平台详细实现计划 | 14 文件变更 + 10 步骤顺序 + 验证方案 |
| `Workflow` | Ultracode 模式 | (本次会话未实际使用，但已启用) | 多 Agent 并行编排 |

### 2.2 专用 Agent 类型

| Agent | 触发条件 | 本次使用次数 | 典型任务 |
|-------|---------|------------|---------|
| `Explore` | 需要跨多个文件/目录搜索 | 2 次 | 探索 backend 种子数据 + API；探索 demo-service 结构 |
| `Plan` | 复杂架构设计 | 1 次 | 设计多域仿真平台的 14 文件 + 8 步实施计划 |
| `general-purpose` | 复杂的多步搜索任务 | — | (备用) |

### 2.3 工具使用频率

| 工具 | 使用次数(估) | 主要用途 |
|------|------------|---------|
| `Write` | 40+ | 新建文件 (14 个 Go 文件 + 前端组件 + 文档) |
| `Edit` | 30+ | 精准修改已有代码 |
| `Bash` | 50+ | 编译、测试、curl 验证、git 操作 |
| `Read` | 25+ | 读取现有代码和文档 |
| `Grep` | 15+ | 搜索函数定义、CSS 类名 |

---

## 三、Prompt 工程实践

### 3.1 设计阶段 Prompt

**Prompt 1: 架构设计**
```
用户: "demo-service可以有业务逻辑实现吗，例如三个服务甚至更多
       模拟航空中的火控雷达，光电中的传感器，船舶中的航海等等，可以实现吗"
```
→ AI 自动进入 Plan Mode → Plan Agent 输出 8 步实施计划 → 用户审批 → 顺序执行

**Prompt 2: 前端架构重设计**
```
用户: "检查当前的前端页面架构...建议切换为左右布局"
```
→ AI 探索代码 → 输出竞品分析 + 组件树 + 交互流程 → 用户多轮迭代 refinement

**Prompt 3: 概念澄清**
```
用户: "配置组 的服务算什么服务" (配置中心的 service vs 物理进程)
```
→ AI 解释两层"服务"概念并给出合并/分离两种方案

### 3.2 编码阶段 Prompt

**Prompt 4: 领域服务实现**
```
Plan Agent: "设计一个详细的实现计划来扩展 /root/fse/demo-service
           从单 config-echo 服务到多域仿真平台"
```
→ Plan Agent 输出: 目录结构 + 文件清单 + 配置契约表 + API 设计 + 端口分配 + 验证方案

**Prompt 5: 精准修复**
```
用户: "配置管理中的列表排序偶尔会乱"
```
→ AI 定位根因 (Go map 随机遍历) → `filteredConfigs` 加稳定排序

**Prompt 6: 按需提取**
```
"提取 ConfigClient 到 pkg/configclient/ 作为共享 SDK"
"main.go 重写为 SERVICE_TYPE 分发器"
```
→ 小步快跑，每步一个清晰目标

### 3.3 调试阶段 Prompt

**Prompt 7: 问题定位**
```
用户: "为什么全离线？不是已经 start.sh 了"
```
→ AI: `ss -tlnp` 检查端口 → 发现 CORS 头缺失 → 老进程占端口 → kill + 重启

**Prompt 8: 回退与修正**
```
用户: "你的折叠按钮能再丑点吗？根本不符合目前的风格啊"
```
→ 接受批评，重写为符合 Linear 风格

### 3.4 文档阶段 Prompt

**Prompt 9: 架构文档生成**
```
用户: "重新梳理整个项目代码文件，输出你的架构文档，核对交付物要求"
```
→ 读取全部源码 → 输出完整 ARCHITECTURE.md (10 节)

**Prompt 10: 交互式报告**
```
用户: "读取该文档，进行优化，前提一定一定不加数据库就本地存储"
```
→ 纯静态 HTML → 交互版: localStorage 持久化 + 折叠 + 深色 + 笔记

---

## 四、Prompt 模式总结

### 4.1 有效模式

| 模式 | 示例 | 为什么有效 |
|------|------|-----------|
| **先问"可以实现吗"再实施** | "可以实现吗" | 给 AI 机会先做 feasibility check，避免盲目实施 |
| **给具体领域场景** | "火控雷达/光电传感器/船舶航海" | 具体场景让 AI 设计出有意义的配置契约和业务逻辑 |
| **一次一个问题** | "列表排序乱" → 精准修复 | 小步修复，每次改动范围可控 |
| **追问概念** | "配置组 vs 配置项的定义" | 澄清术语避免后续理解偏差 |
| **用交付物要求核对** | "核对交付物要求" | 用 checklist 驱动文档补全 |

### 4.2 需要避免

| 反模式 | 问题 | 改进 |
|------|------|------|
| 一次描述多个不相关的需求 | AI 容易遗漏或混在一起 | 拆成独立消息 |
| 模糊的"优化一下" | AI 不知道优化方向 | 给出具体问题 + 预期效果 |
| 不提约束条件 | AI 可能引入不必要的依赖 | "不加数据库" "本地存储" |

---

## 五、工作流阶段

```
第一轮: MVP 搭建
├── Prompt: "开发分布式配置中心 MVP"
├── Skill: 无 (基础编码)
├── 产出: backend + frontend + demo-service 骨架

第二轮: 质量加固
├── Prompt: "TDD 测试" / "lint 修复"
├── Skill: Bash (go test / revive / gofmt)
├── 产出: 36 测试 + 13 lint 修复 + BUGLOG

第三轮: 前端重设计
├── Prompt: "重做前端信息架构"
├── Skill: EnterPlanMode → Explore Agent
├── 产出: 左右分栏 + Dashboard + 响应式

第四轮: 多域仿真
├── Prompt: "火控雷达/光电传感器/船舶航海"
├── Skill: Plan Agent → Explore Agent → 8 步实施
├── 产出: 14 新文件 + 10 配置组 + 3 领域服务

第五轮: 文档 + 测试
├── Prompt: "更新文档" / "更新 test 文件"
├── Skill: Write + Bash
├── 产出: README/ARCHITECTURE/AI_PROMPTS/SKILLS_AND_PROMPTS + 64 测试
```

---

## 六、Skills 效果评估

| Skill/工具 | 效率提升 | 适用场景 | 局限 |
|-----------|---------|---------|------|
| EnterPlanMode | ⭐⭐⭐⭐⭐ | 多文件、架构级改动 | 需用户审批，小改动过度 |
| Explore Agent | ⭐⭐⭐⭐ | 需要跨文件搜索理解 | 只读，不能修改 |
| Plan Agent | ⭐⭐⭐⭐⭐ | 复杂需求的方案设计 | 需要明确的输入上下文 |
| Write (批量) | ⭐⭐⭐⭐ | 新建多个独立文件 | 需人工检查一致性 |
| Edit (精准) | ⭐⭐⭐⭐⭐ | 修改已有文件的小块代码 | 匹配字符串易因空格失败 |
| Bash (验证) | ⭐⭐⭐⭐⭐ | 编译、测试、curl | 命令需用户授权 |

---

## 七、关键决策记录

| 决策 | 背景 | 选择 | 理由 |
|------|------|------|------|
| 内存存储 | 交付要求"无需数据库" | `sync.RWMutex` + Go map | 最简单满足需求，ARCHITECTURE 已说明持久化扩展 |
| Draft/Published 分离 | 模拟 Apollo 编辑≠生效 | 双 map + DeepCopy 保护 | 核心亮点，被评估报告重点肯定 |
| 单一 binary 多服务 | 避免 3 个独立 Go 项目 | `SERVICE_TYPE` 分发 | 代码复用 ConfigClient SDK，部署简单 |
| 无 Vue Router | MVP 3 个 Tab | Tab 切换 | 交付物要求简单，Router 过度设计 |
| 前端直接用 /health 探测 | 跨域问题 | CORS 中间件 + 绝对 URL | 比 Vite 代理更直接，服务独立可验证 |
| localStorage 无数据库 | 评估报告交互版 | 浏览器 localStorage | 用户明确要求"不加数据库" |

---

## 八、Prompt 模板 (可复用)

### 8.1 架构设计
```
我有一个 [项目描述]。现在需要 [具体需求]。
前提约束: [不加数据库/纯前端/...]
请先探索现有代码，然后给出详细的实施计划。
```

### 8.2 Bug 修复
```
[具体现象描述]。请检查代码定位根因并修复。
```

### 8.3 文档生成
```
读取所有代码文件，根据 [交付物要求 check list] 核对并更新文档。
```

### 8.4 概念澄清
```
[术语A] 和 [术语B] 的关系是什么？在系统中如何体现？
```
